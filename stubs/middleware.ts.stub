import type { Request, Response } from '../types'

/**
 * {{Name}} middleware
 *
 * Default-exported, so the framework auto-loads it from src/middleware and
 * applies it to EVERY request. If it should only guard some routes, export it
 * by name instead and pass it to those routes:
 *
 *   // src/routes/api/posts.ts — where make:route generates, so two levels up
 *   import { {{name}} } from '../../middleware/{{lower}}'
 *   router.get('/api/posts', {{name}}, PostController.index)
 */

export default async (req: Request, res: Response, next: () => void) => {
  // Add your middleware logic here

  next()
}

# Test files
*.test.js
**/*.test.js

# Test fixtures. Both are read only by tests, which do not ship, so neither
# should they — the CommonMark suite alone is 140kB.
src/content/commonmark-spec.json
src/storage/aws-sigv4-suite.json

# Development
.claude/
plans/
.git/
.gitignore

# Docs (README is included by default)
*.md
!README.md

# Type generation config (the generated types/ directory itself ships)
tsconfig.types.json

# Misc
.DS_Store
*.log
